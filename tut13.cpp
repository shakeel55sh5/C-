// selection control structure if-else
#include <iostream>
using namespace std;

int main(){
    int age;
    cout<<"Tell me your age"<<endl;
    cin>>age;
    if(age<18){
        cout<<"you are kid"<<endl;
    }
    else if(age==18){
        cout<<"you are younger"<<endl;
    }
    else{
        cout<<"you are legend"<<endl;
    }
    return 0;
}
// Explanation
/*
in above case we simple write user taking id and then write varaibel name 
on starting write if then use () inside write our deand mean write variabel is < or > according to our demand 
then after one code complee then write for many conditions write again else if and same as it is codeing
if nothng need more then in last write else and write output comand and exit from it
*/